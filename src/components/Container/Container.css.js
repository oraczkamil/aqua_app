import styled from 'styled-components/native';
import {container} from "../../utils/theme/colors";

export const Wrapper = styled.View`
    flex: 1;
    align-items: center;
    justify-content:center;
    background: ${container.background};
`;