import React from 'react';
import {ClientForm, Container} from "../../components";

function AddClient() {
    return (
        <Container>
            <ClientForm />
        </Container>
    );
}

export default AddClient;