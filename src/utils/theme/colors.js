const colors = {
    BLUE: 'rgb(0, 122, 255)',
};

const commons = {
    BACKGROUND: '#fff',
}

export const button = {
    background: commons.BACKGROUND,
    border: {
        color: colors.BLUE,
        type: 'solid',
        size: '1px',
    },
    color: colors.BLUE,
}

export const container = {
    background: commons.BACKGROUND,
}

export const input = {
    primary: colors.BLUE,
    background: commons.BACKGROUND,
}

export const error = {
    color: 'red',
}